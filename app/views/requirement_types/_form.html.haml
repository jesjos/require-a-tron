= form_for @requirement_type do |f|
  -if @requirement_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@requirement_type.errors.count, "error")} prohibited this requirement_type from being saved:"
      %ul
        - @requirement_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
